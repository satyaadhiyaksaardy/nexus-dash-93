# API Authentication
# Generate a secure key: openssl rand -hex 32
API_KEY=your-super-secret-key-change-this

# Server settings
HOST=0.0.0.0
PORT=8000

# Monitoring settings
# Mark servers as "down" if no data received for X seconds
STALE_THRESHOLD_SECONDS=300
